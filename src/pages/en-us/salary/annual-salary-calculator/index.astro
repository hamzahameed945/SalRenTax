---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../../../components/navigation/Breadcrumbs.astro';
import InContentAd from '../../../../components/ads/InContentAd.astro';
import HourlyToSalaryCalculator from '../../../../components/islands/HourlyToSalaryCalculator';
import { enUS } from '../../../../i18n/en-US';
import { buildLocalePath } from '../../../../i18n/utils';

const locale = 'en-US' as const;
---

<BaseLayout
  locale={locale}
  title="Annual Salary Calculator — SalRenTax"
  description="Calculate annual salary from hourly pay, weekly hours, and working weeks per year."
  canonical="/en-us/salary/annual-salary-calculator/"
>
  <Breadcrumbs
    locale={locale}
    trail={[{ label: 'Salary Calculators', href: buildLocalePath(locale, 'salary') }]}
  />
  <article class="mx-auto max-w-4xl px-4 py-10">
    <h1 class="text-3xl font-bold text-slate-900">Annual Salary Calculator</h1>
    <p class="mt-3 max-w-2xl text-slate-600">
      Enter an hourly wage and working schedule to estimate annual, monthly, biweekly, weekly, and daily pay.
    </p>
    <div class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
      <HourlyToSalaryCalculator client:load t={enUS} locale="en-US" />
    </div>
    <div class="mt-8">
      <InContentAd slotId="annual-salary-calculator-1" />
    </div>
    <div class="mt-10 prose prose-slate max-w-none">
      <h2>How the annual salary estimate works</h2>
      <p>
        The calculator multiplies hourly pay by hours per week and working weeks per year, then derives monthly,
        biweekly, weekly, and daily equivalents from that annual amount.
      </p>
    </div>
  </article>
</BaseLayout>
