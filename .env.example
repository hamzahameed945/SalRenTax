# Copy this file to .env and set the real production domain before a
# production build. Used for the sitemap `site` URL and for canonical /
# Open Graph / JSON-LD URLs. If unset, the build falls back to the
# placeholder https://example.com so a misconfigured build never claims
# a real domain that hasn't actually been supplied.
PUBLIC_SITE_URL=https://payrentax.me
